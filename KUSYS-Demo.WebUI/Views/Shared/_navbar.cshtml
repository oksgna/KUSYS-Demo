<nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top">
    <div class="container">
        <a class="navbar-brand" href="/">KUSYS</a>
        <ul class="navbar-nav mr-auto">
           
            
            @if (User.Identity.IsAuthenticated)
            {
                if(User.IsInRole("admin")){
                <li class="nav-item">
                <a asp-controller="Home" asp-action="Index" class="nav-link active">Home</a>
                </li>
                <li class="nav-item">
                    <a  asp-controller="User" asp-action="List" class="nav-link">Admin Students List</a>
                </li>
               

                <li class="nav-item">
                    <a asp-controller="Admin" asp-action="CreateStudent" class="nav-link">Admin Add Student</a>
                </li>
                <li class="nav-item">
                    <a asp-controller="Admin" asp-action="Index" class="nav-link">Admin Manage Students</a>
            </li>
          
            }
            else if(User.IsInRole("user")){
            
               

                
                <li class="nav-item">
                    <a asp-controller="User" asp-action="DetailsForUser" class="nav-link"> Students(USer)</a>
            </li>
            }}
        </ul>

        <ul class="navbar-nav mr-auto">
            @if (User.Identity.IsAuthenticated)
            {
                <li class="nav-item">
                    <a asp-controller="Account" asp-action="Logout" class="nav-link">Logout</a>
            </li>
            }
            else{
            <li class="nav-item">
                <a href="#" class="nav-link">Login</a>
            </li>
            <li class="nav-item">
                <a href="#" class="nav-link">Register</a>
            </li>
            }
        </ul>
    </div>
</nav>